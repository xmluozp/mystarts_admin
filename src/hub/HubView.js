import React from 'react';

/** ==========================================================================
 * THIS PAGE: view component
 */
const HubView = ({handleOnSubmit, ...props}) => {
    return (
    <div>

    </div>)
}

export default HubView